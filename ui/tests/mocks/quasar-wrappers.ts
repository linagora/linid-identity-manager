/**
 *  Mocks for Quasar's defineBoot.
 * @param fn - The function to be executed during the boot process.
 * @returns The result of the provided function.
 */
export const defineBoot = (fn: unknown) => fn;
/**
 * Mocks for Quasar's defineRoute.
 * @param fn - The function to be executed for defining a route.
 * @returns The result of the provided function.
 */
export const defineRoute = (fn: unknown) => fn;
/**
 * Mocks for Quasar's defineStore.
 * @param fn - The function to be executed for defining a store.
 * @returns The result of the provided function.
 */
export const defineStore = (fn: unknown) => fn;
